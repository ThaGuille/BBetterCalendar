<?xml version="1.0" encoding="utf-8"?>
<!-- Fila de la lista de proyectos (spec projects-mvp): nombre, barra de % (recuento de items,
     decisión #6 del roadmap), chip de estado del deadline (ámbar/rojo, lenguaje visual de
     app-limits) y una franja de color de acento que cicla la paleta bb_*. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/spacing_sm"
    android:background="@drawable/bg_card"
    android:elevation="@dimen/elevation_card"
    android:orientation="horizontal"
    android:padding="@dimen/spacing_md">

    <View
        android:id="@+id/projectColorAccent"
        android:layout_width="4dp"
        android:layout_height="match_parent"
        android:background="@color/bb_primary" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/spacing_sm"
        android:layout_weight="1"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/projectNameText"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLines="1"
                android:textAppearance="@style/TextAppearance.BBetter.Title" />

            <TextView
                android:id="@+id/projectDeadlineChip"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/spacing_sm"
                android:background="@drawable/bg_chip_secondary"
                android:paddingStart="10dp"
                android:paddingEnd="10dp"
                android:paddingTop="4dp"
                android:paddingBottom="4dp"
                android:textAppearance="@style/TextAppearance.BBetter.Chip"
                android:visibility="gone" />
        </LinearLayout>

        <ProgressBar
            android:id="@+id/projectProgressBar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="6dp"
            android:layout_marginTop="@dimen/spacing_xs"
            android:max="100"
            android:progressBackgroundTint="@color/bb_surface_subtle"
            android:progressTint="@color/bb_primary" />

        <TextView
            android:id="@+id/projectProgressText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_xs"
            android:textAppearance="@style/TextAppearance.BBetter.Label" />

    </LinearLayout>
</LinearLayout>
